#include <iostream>
#include <vector>
#include <string>
#include <sstream>
#include <time.h>
#include <algorithm>

using namespace std;

string zadatak1(string s);

bool vowels(char s);
string ay(string str);
void PigLatin(string* sentence);


vector <int> stuffing_bit(vector <int>& v);

vector <int> unstuffing_bit(vector <int>& v);

void game(vector<int>& vector, int n);
